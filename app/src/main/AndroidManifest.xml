<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.practica4">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_practica4"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_practica4_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".DescripcionJuegoActivity"
            android:parentActivityName=".MainActivity">


        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
<!--   Hace que se vea en el principal y no quiero
         <meta-data-->
<!--                android:name="android.support.PARENT_ACTIVITY"-->
<!--                android:value="com.example.practica4.MainActivity" />-->
        </activity>
    </application>

</manifest>